# Library-Management-System
The Library Management System is a lightweight Java Swing application built for core library administration. Featuring a dual-tab GUI, it handles complete CRUD operations for books and members without external dependencies. The system utilizes simple flat-file IO (books.txt and members.txt) for instant setup and cross-platform portability.
